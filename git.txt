git init
ls -la
open .git
git add * -- and watch folder
git log --oneline
git remote show origin -- nada
Now publish with VSCode
git remote show origin  -- aha
git status
git  branch
git checkout 


git revert  / git revert  --abort
